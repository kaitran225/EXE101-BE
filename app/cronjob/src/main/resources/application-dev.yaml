spring:
  config:
    activate:
      on-profile: dev
  
  datasource:
    url: jdbc:postgresql://${DB_HOST:dev-db.example.com}:${DB_PORT:5432}/cronjob
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  
  cloud:
    task:
      execution:
        pool:
          core-size: 2
          max-size: 10

server:
  port: 8085

logging:
  level:
    org.springframework.scheduling: INFO

